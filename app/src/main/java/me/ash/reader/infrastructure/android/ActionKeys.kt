package me.ash.reader.infrastructure.android

import androidx.glance.action.ActionParameters

/**
 * Common keys we use to define actions in intents.
 */
object ActionKeys {
    val NAV_ROUTE = ActionParameters.Key<String>("navigation_uri")
}